import React from 'react';
import PropTypes from 'prop-types';

SideBar.propTypes = {

};

function SideBar(props) {
    return (
        <div>
            side bar
        </div>
    );
}

export default SideBar;